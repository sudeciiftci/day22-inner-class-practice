# Java Anonymous Inner Class Practice

This project demonstrates **anonymous inner classes** in Java.

## Concept
- An **anonymous inner class** is a class without a name.
- It is created at the same time as the object.
- Used for quick one-time implementations.

## Example
- `Animal` is an abstract class.
- `speak()` is implemented directly when creating objects.
- No separate subclass is needed.
